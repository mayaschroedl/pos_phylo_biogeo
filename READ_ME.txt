ID: project-ID (ex:or)
FILENAMES: multiple project-IDs which I want to align
FILENAMES_array: FILENAMES as an array

need to install:
- all HybPiper dependencies (see Hybpiper)
- bwa
- samtools for covmaps

-dplyr
-ggplot2
&other r packages

put into programms:
Hybpiper

make sure that:
-target file there


RUN SCRIPTS ALWAYS FROM MAIN WORKING DIRECTORY (PROJECT FILE)

enter wd? of trimmed vs of untrimmed

need to have installed:
- python 2
-ete3 for phypart

for chaning tiplabels use: