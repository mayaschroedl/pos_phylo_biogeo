all reads renamed to TAG*_R*_paired.fastq

trimmed reads renamed to TAG*_R*_paired_trimmed.fastq

all reads (trimmed + untrimmed) into folder /1_phylo_reconstruction/renamed_reads

put also target file (named target_file.fasta) into /1_phylo_reconstruction/renamed_reads
put also file (named tags_indiv.txt) with TAGs and corresponding individual name (no duplicates) to /1_phylo_reconstruction/renamed_reads. one column "tags" with TAGs; one column "indiv" with individual names. NO SPACES 